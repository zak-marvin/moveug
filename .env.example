# Copy this to .env and fill in real values. Never commit the real .env.

DJANGO_SECRET_KEY=change-me-to-something-random
DJANGO_DEBUG=True

GOOGLE_MAPS_API_KEY=

MOMO_TARGET_ENV=sandbox
MOMO_COLLECTION_SUB_KEY=
MOMO_DISBURSEMENT_SUB_KEY=
MOMO_API_USER=
MOMO_API_KEY=

# True = payments are faked locally (SUCCESSFUL immediately, no MTN network call).
# Set to False once you're testing against real MTN sandbox/production credentials.
MOMO_SIMULATE=True
